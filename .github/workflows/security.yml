name: Security Scan

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  security-events: write

jobs:
  codeql:
    name: CodeQL Analysis (Go)
    runs-on: ubuntu-22.04
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        language: [ go ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2
        with:
          fetch-depth: 0

      - name: Detect go.mod
        id: find-mod
        run: |
          MODPATH=$(git ls-files go.mod | head -n 1 || true)
          if [ -z "$MODPATH" ]; then
            echo "no_go_mod=true" >> $GITHUB_OUTPUT
            echo "module_dir=." >> $GITHUB_OUTPUT
          else
            echo "no_go_mod=false" >> $GITHUB_OUTPUT
            echo "module_dir=." >> $GITHUB_OUTPUT
          fi

      - name: Skip CodeQL if no Go module
        if: ${{ steps.find-mod.outputs.no_go_mod == 'true' }}
        run: echo "No go.mod detected, skipping CodeQL analysis."

      - name: Setup Go
        if: ${{ steps.find-mod.outputs.no_go_mod != 'true' }}
        uses: actions/setup-go@v5.2.0
        with:
          go-version-file: ${{ steps.find-mod.outputs.module_dir }}/go.mod
          cache: true
          cache-dependency-path: ${{ steps.find-mod.outputs.module_dir }}/go.sum

      - name: CodeQL Init
        if: ${{ steps.find-mod.outputs.no_go_mod != 'true' }}
        uses: github/codeql-action/init@v3.28.0
        with:
          languages: go
          queries: security-and-quality
          build-mode: autobuild

      - name: CodeQL autobuild
        if: ${{ steps.find-mod.outputs.no_go_mod != 'true' }}
        id: autobuild
        uses: github/codeql-action/autobuild@v3.28.0
        continue-on-error: true

      - name: Manual Go build fallback
        if: steps.autobuild.outcome != 'success'
        run: |
          set -euo pipefail
          cd "${{ steps.find-mod.outputs.module_dir }}"
          go mod download
          go build ./...

      - name: CodeQL Analyze
        if: ${{ steps.find-mod.outputs.no_go_mod != 'true' }}
        uses: github/codeql-action/analyze@v3.28.0
        with:
          fail-fast: false

  gosec:
    name: Gosec Analysis
    runs-on: ubuntu-22.04
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Setup Go
        uses: actions/setup-go@v5.2.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Install gosec
        run: |
          go install github.com/securego/gosec/v2/cmd/gosec@v2.22.10
          echo "$(go env GOPATH)/bin" >> $GITHUB_PATH
          gosec version

      - name: Run gosec and generate SARIF
        id: gosec
        run: |
          set -euo pipefail
          SARIF="$GITHUB_WORKSPACE/gosec.sarif"
          echo "Running gosec scan..."
          # 生成 SARIF 文件
          gosec -fmt=sarif -out="$SARIF" ./...
          EXIT=$?
          echo "exit_code=$EXIT" >> $GITHUB_OUTPUT
          echo "sarif=$SARIF" >> $GITHUB_OUTPUT

          # 输出高/中问题到 Actions log
          if [ -s "$SARIF" ]; then
            echo "Gosec scan results:"
            jq -r '.runs[].results[] | "\(.severity): \(.message.text) (\(.locations[].file):\(.locations[].line))"' "$SARIF"
          else
            echo "No gosec issues found."
          fi

      - name: Upload SARIF to GitHub Code Scanning
        if: ${{ steps.gosec.outputs.sarif != '' }}
        uses: github/codeql-action/upload-sarif@v3.28.0
        with:
          sarif_file: ${{ steps.gosec.outputs.sarif }}

      - name: Upload SARIF artifact
        if: ${{ steps.gosec.outputs.sarif != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: gosec-sarif
          path: ${{ steps.gosec.outputs.sarif }}
          retention-days: 7

      - name: Fail on high/medium gosec findings
        if: ${{ steps.gosec.outputs.exit_code != '0' }}
        run: |
          echo "Gosec found High/Medium issues! Failing the job."
          exit 1
